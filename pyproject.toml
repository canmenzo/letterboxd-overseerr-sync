[project]
name = "watchlistrr"
version = "1.0.0"
description = "Watchlistrr - sync a Letterboxd watchlist into Seerr (formerly Overseerr)"
requires-python = ">=3.10"
dependencies = ["requests>=2.31,<3"]

[project.scripts]
watchlistrr = "watchlistrr.__main__:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
